# Environment Configuration
# Copy this file to .env.local and fill in your values

# Node environment
NODE_ENV=production

# Cloudflare Tunnel (optional - only needed if using tunnel)
# TUNNEL_TOKEN=

# Vercel Analytics (already in package.json, no additional setup needed)
